// config
// https://s3.amazonaws.com/s3.helloheart.home.assignment/bloodTestConfig.json
const host = 's3.amazonaws.com';
const suffix = 's3.helloheart.home.assignment/bloodTestConfig.json';
const baseUrl = `https://${host}/`;

export default {
  baseUrl,
  suffix,
};
